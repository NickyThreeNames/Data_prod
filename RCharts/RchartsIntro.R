#Create javascript visualizations using R - no need to learn D3 or javascript
#Additional docs and install available on Github via Ramnath
#install_github('rcharts', 'ramnathv') using devtools 

require(rCharts)
haireye <- as.data.frame(HairEyeColor)

n1

#n1$save('./n1.html', cdn = TRUE)
#cat('<iframe src = './n1.html', width = 100%, height = 600>'</iframe>)
#Lecture is embedding the rCharts into slidify, I will skip that.